# api/.env

# Dev convenience: bypass auth if true
DEV_SKIP_AUTH=true

# Server
PORT=8080
CORS_ORIGINS=http://localhost:3000

# Database (optional for MVP stub)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/sportsdb

# JWT secret (future use)
JWT_SECRET=changeme

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
